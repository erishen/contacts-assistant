# 隐私设计说明（Privacy）

本通讯录 App 以「本地优先」为原则处理真实个人信息（手机通讯录）。以下为本项目的隐私处理原则与落地方式。

## 1. 数据流向原则

- **本地优先**：通讯录读取（`expo-contacts`）仅在设备本地进行，用于展示与筛选，不上传任何服务器。
- **最小外发**：与 LLM 相关的功能（AI 起草 / AI 画像 / 智能查找）只向模型发送「姓名 / 归属地（城市名） / 本次意图与上下文」，**绝不发送手机号本身**，也不发送完整通讯录。
- **密钥隔离**：LLM 的 API Key 仅存于系统安全存储（iOS Keychain / Android Keystore），不落明文文件、不进版本库、不出现在日志。
- **不外传 PII**：不向任何第三方上报设备信息或通讯录内容。

## 2. 可选的本地扩展（自我构建专属，不进公开分发）

> ⚠️ 本节描述的「本地社交数据匹配」功能**仅在操作者本机自我构建（self build）时启用**。公开仓库与对外发布版本（public build）**不含此功能、不含任何社交账号数据**，且编译期已将其从产物中彻底移除。

本项目的某些私有构建可从操作者本机的社交关系导出（备注名 + 账号 ID）生成本地数据文件，用于离线匹配与参考。该本地数据文件：

- **不进版本库**：被 `.gitignore` 排除（规则 `*.local.ts`），禁止 `git add`、禁止提交、禁止随公开仓库分发。
- **不进对外发布包**：对外发布构建在打包阶段将此类本地模块强制解析到脱敏占位（空表 / 虚构示例），并经自动校验脚本（`scripts/verify-no-pii.sh` 反编译产物扫描）确认产物中不含任何真实账号 ID。
- 克隆公开仓库后不存在该本地文件，应用仍可正常构建运行（仅相关本地匹配不会命中）。

## 2.1 本地参考草稿的特殊处理

离线参考草稿（基于本机社交聊天生成的风格范例）同样属于上述「自我构建专属」本地数据：

- 由操作者本机脚本从本地聊天归档生成为私有文件，**不进版本库、不随公开仓库分发**。
- 仅在「本机构建含有该私有文件」时，生成式 AI 功能才可将草稿原文作为风格范例（few-shot）随提示词外发——这是个人单机、数据均为操作者本人、经知情同意的保留项。
- 对外发布 / 干净克隆不含该文件，相关调用返回空、**默认不执行任何外发**。红线是**绝不发送完整手机号、邮箱、账号 ID，也不发送完整通讯录或多人批量数据**。

## 3. 贡献 / 分发前检查

- 确认 `git status` 中**不出现**任何本地私有数据文件（如 `*.local.ts`）、任何 `*.csv`、`.env`、以及签名密钥 `*.keystore`。
- 切勿将真实社交关系数据、真实通讯录导出、真实参考草稿提交到公开仓库。
- 如需公开分享本项目，请仅保留脱敏占位文件与代码本身。
- ⚠️ **CSV 导出文件保存在本机文件系统**：导出含完整手机号等 PII，虽被 `*.csv` 排除不会进 git，但导出后可能被文件传输 / 云备份 / 其他读存储的 App 拿走。导出后请妥善保管、勿转发，用毕及时删除。

## 4. 合规边界

- 本项目为个人单机实验 App，所有真实 PII 仅存在于设备本地与操作者本机文件系统。
- **运行期外发范围（操作者知情同意）**：调用 LLM 时，提示词允许包含「姓名 / 推断单位 / 手机号归属地（城市名） / 本次目标」。涉及本地社交数据的部分仅限自我构建、且仅在本地私有文件存在时按需使用（见 §2.1），对外发布版无此路径。
- ⚠️ **分发护栏**：本地社交扩展仅在「本机构建含有私有文件」时触发；公开仓库 / 干净克隆不含该文件，相关代码路径在对外发布构建中被编译期移除，**天然安全，无需改代码**。
